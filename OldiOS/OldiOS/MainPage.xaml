<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:OldiOS"
             xmlns:shared="clr-namespace:OldiOS.Shared;assembly=OldiOS.Shared"
             x:Class="OldiOS.MainPage"
             BackgroundColor="{DynamicResource PageBackgroundColor}">

    <AbsoluteLayout>
        <!-- Blazor Web View (always visible) -->
        <BlazorWebView x:Name="blazorWebView" 
                       HostPage="wwwroot/index.html"
                       AbsoluteLayout.LayoutBounds="0,0,1,1" 
                       AbsoluteLayout.LayoutFlags="All">
            <BlazorWebView.RootComponents>
                <RootComponent Selector="#app" ComponentType="{x:Type shared:Routes}" />
            </BlazorWebView.RootComponents>
        </BlazorWebView>
        
        <!-- Safari Native WebView (hidden by default, overlays when active) -->
        <WebView x:Name="safariWebView"
                 IsVisible="False"
                 AbsoluteLayout.LayoutBounds="0,0,1,1" 
                 AbsoluteLayout.LayoutFlags="All" />
    </AbsoluteLayout>

</ContentPage>
